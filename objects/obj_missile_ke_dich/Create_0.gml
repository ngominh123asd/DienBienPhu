// Scale down the fireball missile so it looks beautiful
image_xscale = 0.5;
image_yscale = 0.5;

// Play custom fireball sound
audio_play_sound(sndDragonfire, 10, 0);

// Initialize speed
speed = 3.5;
direction = 270; // Start moving downwards
